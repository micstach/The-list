<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <link href="../css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="../css/main.css" rel="stylesheet" type="text/css" >
    <link href="../css/credentials.css" rel="stylesheet" type="text/css" >

    <link rel="icon" type="image/png" sizes="16x16" href="/res/todo_16.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/res/todo_32.png">
    <link rel="icon" type="image/png" sizes="64x64" href="/res/todo_64.png">  

    <script src="../libs/jquery.min.js"></script>
    <script src="../scripts/register.js"></script>  
  </head>
  <body>
    <a href="/">
      <div class="application-header"></div>
    </a>
    <div class="container centered">
    <% if (locals.verificationSent) { %>
      <% if (verificationSent === 'true') { %>
        <div class="alert alert-success" role="alert"><strong>Brawo!</strong> E-mail z zaproszeniem został wysłany na adres <i><%= email %></i>. <br/><br/>Sprawdż skrzynkę pocztową aby kontynuować rejestrację.</div>
      <% } %>
      <% if (verificationSent === 'false') { %>
        <div class="alert alert-danger" role="alert"><strong>Ups!</strong> Błędny adres email <i><%= email %></i></div>
      <% } %>
    <% } else { %>
      <% if (locals.id) { %>
        <form action="/register?id=<%= id %>" method="post" class="message-form">
      <% } else { %>
        <% if (locals.invalidRequestId) { %>
          <div class="alert alert-danger" role="alert"><strong>Ups!</strong> Niepoprawny link rejestracji, wygeneruj poprawny link wpisując adres e-mail</div>
        <% } %>        
        <form action="/register" method="post" class="message-form">
      <% } %>
          <div class="form-group">
            <label for="email">E-mail</label>
            <% if (locals.email) { %>
                <input name="email" class="form-control input-default" placeholder="Podaj adres e-mail" readonly="readonly" value="<%= email %>">
            <% } else { %>
                <input name="email" class="form-control input-default" placeholder="Podaj adres e-mail">
            <% } %>
            <% if (locals.email_error) { %>
              <div class="alert alert-danger" role="alert"><%= email_error%></div>
            <% } %>        
          </div>
          <% if (locals.id) { %>
            <div class="form-group">
              <label for="user">Nazwa użytkownika</label>
              <% if (locals.user) { %>
                <input name="user" class="form-control input-default" placeholder="Podaj nazwę użytkownika" value="<%= user %>">
              <% } else { %>
                <input name="user" class="form-control input-default" placeholder="Podaj nazwę użytkownika">
              <% } %>
            </div>
            <% if (locals.user_error) { %>
              <div class="alert alert-danger" role="alert"><strong>Ups!</strong> <%= user_error %></div>
            <% } %>        
            <div class="form-group">
              <label for="pwd">Hasło</label>
              <input type="password" name="pwd" class="form-control input-default" placeholder="Podaj hasło">
            </div>
            <div class="form-group">
              <input type="password" name="re-pwd" class="retyped form-control input-default" placeholder="Powtórz hasło">
            </div>
            <% if (locals.error) { %>
              <div class="alert alert-danger" role="alert"><strong>Ups!</strong> <%= error %></div>
            <% } %>
            <button type="submit" class="form-control btn btn-primary"><strong>Zarejestruj się</strong></button>
          <% } else { %>
            <button type="submit" class="form-control btn btn-primary"><strong>Wyślij zaproszenie...</strong></button>
          <% } %>
        </form>
      <% } %>
    </div>  
  </body>
</html>